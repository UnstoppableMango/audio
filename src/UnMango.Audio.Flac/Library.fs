namespace UnMango.Audio.Flac

open System.IO

module Flac =
    let read thing = id

    let run () =
        let file = File.OpenRead("")
        let test = StreamReader(file)
        // let test2 = test.Read
